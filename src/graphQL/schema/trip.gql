type Trip{
    tId: String!
    userid: String!
    fId: String!
    mealOption: String!
    class: String!
    seatNo: String!
}

extend type Query{
    getAllTrips: [Trip!]!
    getUserTrips(uId: String!): [Trip!]!
    getTripById(ID: String!): Trip
}